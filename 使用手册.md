# 使用手册（三步上手）

> 这份文档只讲**怎么用**。原理、架构、设计取舍都在 [README.md](README.md)，不用看也能跑起来。

---

## 第 1 步：启动网关（双击一次，窗口可关）

双击这个文件：

```
scripts\start_gateway.cmd
```

启动脚本会**先自动检查 8317 端口**：

- 如果是**本程序上次的旧实例**还占着端口 → 自动结束它，然后正常启动（直接双击就行，不用先去关旧窗口）
- 如果是**其他程序**占用 → 打印占用进程的 PID 和名字，**不会动它**，按提示处理（换端口或手动关掉那个程序）

窗口会打印一段英文说明（端口 / Host / 托盘位置），然后**等网关真正监听端口**（最多 30 秒）
再自动开浏览器并关窗——所以正常情况下窗口会多停几秒，这是它在确认启动成功，不是卡住。
网关本体**收起在底部系统托盘**，日志不进这个窗口，在 `data\gateway.log`。

如果启动失败，窗口**不会自动关**，会留在原地打印原因：

- `ERROR: the tray exited immediately (code N)` + 日志末尾几行 → 托盘进程自己起不来，
  最常见是 `.venv` 没跟上依赖（末尾若出现 `ModuleNotFoundError`，跑一次 `uv sync` 即可）；
  托盘进程自己的输出在 `data\tray_gateway.log`
- `the gateway did not answer` → 托盘活着但端口没起来，看 `data\gateway.log`

**日常管理不用盯黑窗口**——浏览器打开管理控制台：<http://127.0.0.1:8317/ui>。

**托盘图标**（右下角小圆点）：
- **绿色** = 运行中，**蓝色** = 已停止/卡住
- 右键：查看日志 / 打开控制台 / 重启 / 退出

验证一下是否正常，浏览器打开 <http://127.0.0.1:8317/health>，看到 `"status": "healthy"` 就成了。

**换端口**：默认 `8317`（选它就是因为冷门，不容易跟别的程序打架），两种方式任选：

1. **临时换**（只对这一次启动生效）：命令行运行 `scripts\start_gateway.cmd 9000`
2. **永久换**：改 `.env` 里的 `ZKAI_PORT=9000`，然后重启网关（双击启动也读它）

**让局域网里其他电脑也能用**：`.env` 里 `ZKAI_HOST=0.0.0.0`（默认已是），首次启动时
Windows 防火墙弹窗点「允许访问」。其他电脑**不需要装任何东西**，把客户端（OpenAI SDK、
Cherry Studio、沉浸式翻译等任意兼容工具）的 `base_url` 指到 `http://<本机IP>:8317/v1`
即可，本机 IP 用 `ipconfig` 查看。

**拿到一台新电脑，怎么跑起来**（git clone 或拷源码都一样）：

1. 装 uv（一次性。**Python 都不用装**，它联网时自动下载 3.12+）——PowerShell 执行：
   `irm https://astral.sh/uv/install.ps1 | iex`
2. 双击 `start_gateway.cmd`：脚本发现 `.venv` 缺失或损坏会**自动执行 `uv sync` 重建**
   （第一次要下载依赖，几分钟，需联网；数据库建表在启动时也是自动的）
3. 浏览器打开 <http://127.0.0.1:8317/health> 验证，然后按前面说的配置局域网/换端口

首次启动时，`.env` 和 `config/*.yaml` 不存在会**自动从 `.example` 模板建一份**
（都是 gitignore 的本地文件），然后列出还差哪些值：`ZKAI_ADMIN_TOKEN` 和至少一个
供应商 Key 要自己填，填完重启。想从旧电脑搬 `.env` 就直接拷过来覆盖，效果一样。

### 一键换机（把整套 Key 和配置搬到新电脑）

在**旧电脑**上双击 `scripts\export_machine.cmd`，设一个迁移密码（输入时不显示）。
它会把 `.env`（所有 Key）、`config/*.yaml`、数据库（用量/会话/限额账本）打成一个
**加密**的 zip，存到 `exports\` 并自动打开文件夹。整个过程**不停网关、不删任何东西**。

把那个 zip 拷到新电脑（U盘/网盘/微信都行），然后在新电脑上：

1. 拿到代码（git clone 或拷源码），装 uv：`irm https://astral.sh/uv/install.ps1 | iex`
2. 双击 `scripts\start_gateway.cmd` 跑一次（让它建好 `.venv` 和空的 `.env`）
3. 双击 `scripts\import_machine.cmd`，把 zip 拖进黑窗口，输入刚才的密码
4. 再双击 `start_gateway.cmd` 启动，浏览器开 <http://127.0.0.1:8317/health> 验证

导入时如果目标文件已存在会**先备份到 `imports_backup\`** 再覆盖，后悔药有。
要带着历史用量/会话一起走就导数据库（默认带）；只要 Key 和配置，删掉包里的
`data/zkai.db` 再导入也行（包是标准 zip）。还原数据库时，目标机上残留的
`data\zkai.db-wal` / `-shm` 会一并备份后删除——它们属于被替换掉的上一代数据库。

**ChatGPT / Codex 桌面版也一起搬**：迁移包里有 `config\chatgpt.yaml`（桌面版期望配置），
导入成功后会自动 patch 新电脑的 `~/.codex/config.toml`（只动我们的几个键，app 自己的
mcp_servers/plugins/projects 段落原样保留，旧文件备份成 `config.toml.bak-时间戳`），并把
`.env` 里的 `ZKAI_API_TOKEN` 写进 Windows **用户级**环境变量（黑窗口会打印旧值备份位置和
还原命令，写完还有回读校验）——新电脑装好 ChatGPT 桌面版，打开就在用网关的 Key 池，不用
再手工抄配置。哪怕 `~/.codex` 是你从旧机器整个手抄过来的（包里没有 chatgpt.yaml），这个
用户级环境变量也会按 config.toml 里实际引用的 `env_key` 自动配好。

**红线**：迁移包和 `imports_backup\` 都含全部 Key，已加进 `.gitignore` 不会进仓库，
但别随手发到公开群或传到不信任的机器。

**常见问题**：报「密码不对，或迁移包已损坏」→ 密码输了两次不一致，或拷文件时
zip 被截断，重新输一次或重新拷；报「目标机器上已有这些文件」→ 目标已有配置，
`import_machine.cmd` 会当场问你 `Retry with --overwrite? [y/N]`，输 `y` 就带
`--overwrite` 重跑（旧文件先备份）；报「网关正在端口 XXXX 上运行」→ 先从托盘
右键退出网关再导入。

两个换电脑最常见的坑：

- **`.venv` 文件夹千万别从旧电脑整个拷过去**——里面的 python.exe 是个跳板程序，
  内部指向旧电脑的 Python 安装路径，拷过去必报
  `uv trampoline failed to spawn Python child process`。把 `.venv` 删掉再双击启动即可
- **导入前必须先停网关**——网关开着时覆盖 `data\zkai.db`，SQLite 手里那份
  `-wal` 就对不上新库了，下次启动直接 `database disk image is malformed`（不是警告，
  是真损坏，用量历史会读不出来）。命令行导入现在会自己探测端口并拒绝，双击导入
  同样会先报这一条

**日常管理不用盯黑窗口**——启动后**浏览器会自动打开控制台**，并带上 `.env` 里的
`ZKAI_ADMIN_TOKEN`，不需要手动输令牌；令牌只在改过 `.env` 后变了才要重粘一次
（点右上角「🔑 令牌」）。右上角还有个「🤖 ChatGPT」按钮，管 ChatGPT / Codex
桌面版的客户端配置（改 base_url / 模型 / 思考强度 / 切回官方，见 §3.4）。

控制台是「**左边导航、右边内容**」的布局：左边选要看什么，右边是数据。右上角还能
切浅色 / 暗色、设自动刷新间隔。五个视图：

| 视图 | 能干什么 |
|---|---|
| 总览 | 五个大数字卡（可用凭据 / 池成功率 / 60 分钟失败率 / 7 天 Token / 等价成本）+ 每个供应商一张卡；一键健康检查 / 重载配置 / 清空冷却；**顶部「需要处理」把待手动的配置问题一次列清，每行带一键修复按钮** |
| 凭据池 | 每把 Key 的状态徽章 + 优先级 + 配额用量 + 冷却倒计时，可单把「禁用 / 启用 / 解除冷却」；📌 表示当前钉住的会话数 |
| 请求记录 | 按状态/供应商/别名/模型/错误筛选、翻页；点任意一行看**每一次尝试**的上游错误原文（中文 + tooltip 里保留原文） |
| 用量统计 | 按天/供应商/模型/凭据的 token 与成本 |
| 模型与别名 | 模型→部署→供应商全景 + 「路由预览」（不花钱就能看到某请求会怎么走）+ 模型市场 |

界面和接口报错都是中文；`error_type` 之类的机器码和状态码不变，脚本和 API 调用方
该匹配什么还匹配什么。

---

## 第 2 步：改配置（只在需要时做）

### 2.1 加一把新 Key

打开 `.env`，找到对应段落，把 Key 粘在等号后面：

```ini
SENSENOVA_API_KEY_07=sk-你的新Key
```

然后在 `config/providers.yaml` 的 `sensenova` 段落末尾**照抄一段**：

```yaml
      - id: sensenova-07
        env: SENSENOVA_API_KEY_07      # 这里填「变量名」，不是 Key 本身
        priority: 76                    # 数字越大越先用
        weight: 1.0
        tags: ["account-f"]             # 如果这是新账号，写个新标签
        max_consecutive_failures: 3
```

**改完必须重启网关**（托盘图标右键 → 退出，再重新双击 `scripts\start_gateway.cmd`）。

### 2.2 增减供应商

供应商 = 上游协议 + 接入地址（`type` + `base_url`）。增减走控制台，不用改文件。

**加一个新供应商**：

1. 打开控制台 <http://127.0.0.1:8317/ui> → 「凭据池」→ 点右上角「🏢 供应商」
2. 点「➕ 新建供应商」，填三项：
   - **供应商 ID**：自己起个英文短名，比如 `my-openai`（后面模型、别名都引用它）
   - **协议类型**：绝大多数选 `openai_compatible`；只有 Anthropic 官方才选 `anthropic`
   - **接入地址**：上游的 `base_url`，**必须带 `/v1` 结尾**（比如 `https://api.example.com/v1`）
3. 点保存 → 立即生效并写进 `config/providers.yaml`，**紧接着自动弹出加 Key 窗口**
   （选好供应商后 ID 和变量名会自动起好，你只要把 Key 粘进去；Key 本体写进 `.env`，
   `providers.yaml` 里只记变量名，之后换机器不用重新配）
4. 加完 Key，总览页的「⚠️ 需要处理」会提示"一个模型都没挂上"，点「🧺 去模型市场挑」

**加模型是"挑"不是"填"**：模型市场会调供应商的 `GET /models`，把它真实支持的模型列出来。
上游愿意给元数据的（StepFun 会给上下文上限、支不支持图像、思考强度档位），表单顶部会显示
**「📋 供应商实测能力」**面板逐条解释含义，并自动按实测值填好上下文窗口和能力评分——
实测过的不支持项直接禁用，不用你判断。上游只给个 id 的（NVIDIA、魔搭），则按经验值预估，
表格里会标「预估」而不是「实测」，你按需微调。

**编辑供应商**：列表里点「编辑」，只能改协议/地址/超时/开关——**不会动已配的 Key**（Key 在凭据池单独管理）。

**删供应商**：列表里点「删除」。如果还有模型部署在这个供应商上，会提示你先把那些模型删掉或挪走，再回来删。

**临时不用某个供应商**：把「启用」关掉就行（不用删，Key 保留）。

### 2.3 换模型 / 改优先级

**只有一个地方管模型名：`config/models.yaml`**。`.env` 里写 `XXX_MODEL=` 是**完全无效**的（程序不读，也不报错）。

| 想改什么 | 改哪里 |
|---|---|
| 某个模型走哪个上游、上游叫什么名字 | `models[].deployments[].model` |
| 别名叫起来用哪些模型、谁优先 | `aliases[].targets`（**顺序就是优先级**） |
| 用哪把 Key | `providers[].credentials[].env`（填变量**名**） |

比如让 `zk-coding` 换个顺序，就是编辑 `config/models.yaml` 里这一行：

```yaml
  - name: zk-coding
    targets: [kimi-k3, glm-5.2, deepseek-v4-flash, ...]
    #        ↑ 排第一的就是首选，挂了自动往后
```

### 2.4 关于商汤的 Key 与账号

现役 **7 把 Key = 7 个账号，每账号正好一把**（03~09；01/02 原本同属账号 A，
2026-09-20 已禁用，`config/providers.yaml` 里两条僵尸条目待清理）。

- **额度按账号算**：跨账号各自独立 → 现役 **7 份**独立额度。
- **网关会自动用上**：某把 Key 撞 429 就只冷却它自己，下一把立刻顶上，跨账号的额度会自动叠加，**不需要额外配置**。
- `tags: ["account-x"]` 只是给人看的标签（比如 `/admin/health` 里能看出来），**不影响调度**。

> 429 时网关会把多个账号逐把试一遍：`retry.max_credentials_per_deployment`
> 现设成 `8`（≥ 账号数 7，全可达；加账号时同步 +1，**删这行不会取消限制**——
> 会回落到代码默认 3）。

---

## 第 3 步：调用

### 3.1 用 curl / 浏览器

```bash
curl http://127.0.0.1:8317/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"zk-coding","messages":[{"role":"user","content":"写一个快排"}]}'
```

> Windows 的 `cmd` 里引号转义麻烦，直接用 Git Bash 或换下面的 Python 方式。

### 3.2 用 Python（OpenAI SDK）

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://127.0.0.1:8317/v1",
    api_key="zk-ai-local",          # 本地网关不校验，随便填
)

resp = client.chat.completions.create(
    model="zk-coding",              # 用别名，别写真实模型名
    messages=[{"role": "user", "content": "写一个快排"}],
)
print(resp.choices[0].message.content)
```

### 3.3 用 ZCode

已经注册好了（provider 名叫 `ZK-AI Gateway`）。打开 ZCode → 设置 → 模型，选它，然后模型名填 `zk-coding` 即可。

如果没注册过，跑一次这个（会先备份配置）：

```bash
.venv\Scripts\python.exe scripts\setup_zcode.py --apply
```

### 3.4 用 Codex（ChatGPT 那个 CLI / 桌面版）

Codex CLI 0.154 起只认 Responses API（`wire_api = "chat"` 已被移除），网关的
`/v1/responses` 已支持完整工具调用闭环（2026-09-20 实测：写文件 → 执行 → 验证）。
在 `~/.codex/config.toml` 里加一个 provider（纯增量，不动 ChatGPT 登录态）：

```toml
model = "zk-auto"                 # 或 zk-k3 / 具体模型名
model_provider = "zkai"

[model_providers.zkai]
name = "ZK-AI"
base_url = "http://127.0.0.1:8317/v1"
wire_api = "responses"
env_key = "ZKAI_API_TOKEN"        # 网关没设令牌时随便填；设了就得填真的
```

Windows 下要能执行命令，保持 `[windows] sandbox = "elevated"`（或跑
`codex exec` 时加 `--sandbox danger-full-access`）。注意：**不需要 ChatGPT 账号**，
用的是网关自己的 Key 池。

**配置修改不用手工编辑文件**——控制台右上角「🤖 ChatGPT」面板（或
`PUT /admin/chatgpt/client`）直接改上面这些项：模型、provider 表名、`base_url`、
`wire_api`、`env_key`、思考强度，以及**一键切回官方模型**（删 `model_provider` 行、
`model` 改成官方名；provider 表保留，随时切回来）。保存前有**预览变更**，写入前自动
备份 `config.toml.bak-时间戳`，app 自己的段落（mcp_servers / plugins / projects 等）
一个字节都不会动。期望配置存在 `config/chatgpt.yaml`，**换机时随迁移包一起走**——
新电脑导入后桌面版自动配好，打开即用（详见第 1 步「一键换机」）。

**换模型 / 思考强度**：这个版本的桌面版**没有模型选择器**（2026-09-20 翻遍整个
界面树确认），模型只由 config 的 `model = "..."` 决定——用上面的面板改，或改这一行
再重启 app。可选值：别名 `zk-auto` / `zk-k3` / `zk-vision`，或具体模型名（`kimi-k3`、
`step-5-preview`、`deepseek-ai/DeepSeek-V4.1-Flash`、`glm-5.3` 等）。
思考强度在输入框旁「自定义 ◯级」按钮里换（会写回 config 的
`model_reasoning_effort`）；CLI 里用 `-m 模型名` 更灵活。

**桌面版报 `404 model 'xxx' is not configured`？** 说明客户端发的模型名网关
里没有——最常见的是 `~/.codex/config.toml` 的 `model` 被写成了一个不存在的名字
（换机时可能带过去）。修法：控制台「🤖 ChatGPT」面板把「模型」改成 `zk-auto`
（或别名/模型清单里的名字，下拉有提示），点「保存并写入」；报错文案里也会列出
相近的可用名（比如把 `zk` 误写成模型名时，会提示 `zk-auto/zk-k3/zk-vision`）。
注意保存时面板就会拦截不存在的模型名并列出可用项——这是为了防止把 404 配置
写进去。

**桌面版报 `Missing environment variable: ZKAI_API_TOKEN`？** 桌面版从资源管理器启动，
只从 Windows **用户级**环境变量读 Key（不读 `.env`、不读命令行窗口的变量）——换机器、
或在别的窗口里 `set` 过，都可能出现这句报错。三种修法，任选其一：

1. 控制台「🤖 ChatGPT」面板：状态条会把这一项标红，点「🔧 同步令牌到用户环境变量」，
   然后**重启桌面版**（右键托盘图标退出，再打开）；
2. 项目目录下 PowerShell 执行（值直接从 `.env` 取，不用手抄）：
   `[Environment]::SetEnvironmentVariable('ZKAI_API_TOKEN',(Get-Content .env | Select-String '^ZKAI_API_TOKEN=').ToString().Split('=',2)[1],'User')`
3. 换机导入现在会自动写这个变量并回读校验（黑窗口有输出）——手抄 `~/.codex` 的场景也覆盖；
   导入时 `.env` 里缺这个值，输出里会直接给 `setx` 命令。

**控制台热切换（不用重启 app）**：网关控制台的「🤖 ChatGPT」按钮（或 `POST /admin/chatgpt`）
把所选模型提到 `zk-auto` 链首——下一条消息就走新模型，不用改 `config.toml` 也不用重启桌面版。
注意：`zk-auto` 用能力分排序（`strategy=capability`），只调 targets 顺序没用；切换会顺手把该
模型设为 `pin_first`，这一下才能盖过能力分，让它真正领先。

---

## 后台烧 flash-lite 积分（换 K3 积分）

商汤的积分池规则（关键，决定了怎么烧才不亏）：

- Flash-lite 扣减顺序：**专属池周期积分 → 通用池周期积分 → 通用池活动积分**；
  kimi-k3 等其他模型**只扣通用池**。只有烧**专属池**的部分才按 1:1 折算回充成
  通用池活动积分（30 天有效，K3 可用）；一旦溢出扣到通用池，就是直接烧 K3 的口粮，纯亏。
- 池的规格：专属池与通用池各 **60 万积分/周、每 5h 滚动窗口 6 万积分**（每账号）。
- 溢出是**静默**的：专属池窗口烧满后请求照常成功、自动切到通用池扣，API 不报错、
  响应里也看不出扣的哪个池（兼容层也没有余额查询接口）。

所以消耗器的策略是**按积分记账 + 预算熔断**，宁可少烧，绝不溢出：

- **5h 窗口**：滚动记账，上限 6 万 × 安全系数，只防瞬时爆发；
- **周窗口（2026-09-19 改为固定窗口）**：从 `--week-anchor`（默认周一 00:00，可对齐
  控制台重置时刻）起**累计**本周烧量，到线就停靠到下周同一时刻。旧版按滚动 7 天记账
  会「遗忘」一周前的消耗，加上费率取了实测区间下沿，熔断线物理上永远触不到——
  2026-09 实测 26 小时烧 ≈13 万积分/账号，真实费率在区间上沿时正好烧穿专属池、
  无感吃掉 K3 的通用池积分（本次事故的根因）；
- **安全系数默认 0.45**（旧 0.9）：内建 2 倍费率不确定性（实测区间 出333~720，
  估算取 360）——即使实际费率是估算的 2 倍，实扣也不会超过官方周上限；
- **绝对上限 `--pool-total-credits`**（默认关）：账号累计烧到该值永久停靠，
  赠送池过期后建议设上，防消耗器空转烧通用池。

**双击 `scripts\start_burner.cmd`** 就行。消耗器会**收起到底部系统托盘**（右下角小圆点），任务栏不占位置，也不容易误碰：

- **绿色** = 运行中，**蓝色** = 已停止/卡住
- 右键图标：查看日志 / 重启 / 退出（账本每分钟自动保存，最多丢 1 分钟记录）

- 不走网关、不影响网关统计，也不依赖网关开着；直连商汤，不占系统代理
- 自动用 `.env` 里现役 7 把 Key（每账号一把）并行烧；撞 429 只冷却那一把
- **自适应并发**：全局 ≤128、单账号 8 起步上限 24；撞 429 目标减半，静默 1 分钟 +1 档，
  自动贴着商汤的限流边界跑；**学到的并发目标重启不丢**（存在 `data/burn_state.json`）
- **账本持久化**：累计 token/积分跨重启连续，和控制台的记账口径对得上
- 实时明细在 `data\burn_sensenova.log`，每 60 秒一条汇总（token 速率 + 各账号窗口用量）
  （托盘菜单「查看日志」打开的就是它；进程原始的 stdout 落在
  `data\burn_sensenova.console.log`，只在排查启动失败时才需要看）

**费率说明**：积分↔token 换算率官方没有公开接口。默认 入120 / 出360 积分每百万 token，
是 2026-09-12 两次控制台实测交叉验证的结果（实际 ≈111/333，区间 111~240 / 333~720），
显示的「≈积分」与控制台实扣基本一致。想再精确就精校准：控制台「积分消耗明细」选与
`data/burn_state.json` 覆盖范围一致的时段，把实扣积分数填进来：

```bash
.venv\Scripts\python.exe scripts\burn_sensenova.py --calibrate-actual 实扣积分数
```

想再快（默认已较激进，429 频繁就别加）：

```bash
scripts\start_burner.cmd --per-account-max 32 --concurrency 192
# 6.7 和 6.8 同属 Flash-lite 专属池、限流桶大概率独立，可再开一个实例并行烧：
scripts\start_burner.cmd --model sensenova-6.7-flash-lite --log-file data\burn_67.log
.venv\Scripts\python.exe scripts\burn_sensenova.py --help   # 全部参数
```

**窗口刷新时间（每账号不一样？）**：控制台显示的「重置时间」有两种可能——

1. **滚动窗口**（官方文档口径）：重置时间 = 当前窗口烧量全部过期的时刻 ≈ 我们烧干它的时刻 + 5h，**随烧随变**。这种情况下消耗器现有逻辑已是最优，什么都不用做。
2. **固定锚点窗口**：每账号的重置时刻相对固定（比如永远 07:15），边界一过 6 万积分瞬间回满。这时最优烧法是「边界后满血爆发烧干再停靠」。

判定方法（零成本，看一眼）：挑一个账号等它被烧干后，隔天再看控制台的重置时间——**还在漂移**（≈烧干时刻+5h）就是滚动；**固定不变**就是锚点。

确认是锚点后，把每个账号的重置时刻告诉消耗器（数字 = Key 序号，1 即不带后缀的 `SENSENOVA_API_KEY`，和 02 同账号共用）：

```bash
scripts\start_burner.cmd --anchors "1=03:30;3=07:15;4=11:40;5=16:20;6=21:00;7=02:30;8=09:10;9=18:45"
```

锚点会存进 `data/burn_state.json`（只需填一次）。安全说明：API 看不出扣哪个池，自动识别不可行；锚点模式下若锚点不准会在错误的时间爆发、静默扣到通用池——所以务必以控制台显示为准填，拿不准就保持默认滚动模式（保守只会少烧）。

实测量级参考：2000~4000 tok/s 输出（8 账号合计），折合 **约 5k~12k 积分/小时**（实扣口径），
取决于商汤限流松紧。在默认周预算（60 万 × 0.45 ≈ 27 万积分/账号）下，全速烧到
周中前后就会停靠等下周锚点，属**预期行为**（宁可少烧）；想贴上限烧，把
`--safety-margin` 提回去即可，但请先确认控制台里专属池的实际规模与重置时刻。

若日志出现「⚠ 溢出实锤：……专属池已被烧穿、通用池/活动池已受损」，说明预算已经
不够保守，请立即到控制台核对专属池实际规模，收紧 `--weekly-credits` / `--safety-margin` /
`--week-anchor`，或设 `--pool-total-credits`。

想开机自启（可选）：以管理员运行一次

```bash
schtasks /Create /TN "ZK-AI burner" /TR "\"D:\zhangkun\AI\ZK-AI\scripts\start_burner.cmd\"" /SC ONLOGON /RL LIMITED
```

---

## 该用哪个别名？

| 别名 | 什么时候用 | 首位模型 |
|---|---|---|
| **`zk-coding`** | **写代码（你最常用的）** | `kimi-k3` → GLM-5.2 → DeepSeek |
| `zk-auto` | 通用对话，什么都行 | `kimi-k3` → GLM-5.2 → DeepSeek |
| `zk-reasoning` | 需要深度推理、分析 | `kimi-k3` → GLM-5.2 |
| `zk-vision` | 看图、OCR | `kimi-k3` → 6.8 Lite |
| `zk-long` | 超长上下文（>256K） | `kimi-k3` → DeepSeek |
| `zk-fast` | 要快，不在乎模型 | 6.8 Flash Lite |
| `zk-cheap` | 省额度，不碰 K3 | 6.8 Flash Lite |
| `zk-k3` | **只想确认 K3 通不通** | 只用 K3，不通就报错 |
| `zk-lite` | 想绕开 K3 的 429 波动 | 6.8 Flash Lite |
| `zk-local` | 免费额度兜底 | Nemotron（NVIDIA）+ 魔搭 |

**不确定就用 `zk-coding`** —— K3 能用就 K3，不能用自动往下兜，请求不会失败。

---

## ZK-Agent 任务台（浏览器里的批量任务跑批器）

**浏览器打开 `http://127.0.0.1:8317/ui/agent`**（或系统托盘网关图标右键 →「打开 Agent」）。
给一句话任务，它自己循环「思考 → 调工具 → 再思考」，全程在页面上流式可见：

```
你：把 scripts 下所有启动脚本的端口说明改成 8317，跑一遍 pytest 验证
Agent：read_file → grep → write_file（弹窗请你批准，附 diff）→ run_command pytest → 总结
```

- **工具**：读文件/列目录/搜索/grep 直接执行；**写文件与跑命令必须你批准**
  （弹窗带 diff 或命令原文，可点「批准且本会话不再询问」；`rm -rf`、`git push`、
  `format` 等破坏性命令被硬黑名单拦截，批准了也不执行）
- **工作区**：默认本仓库根目录（`ZKAI_AGENT_WORKSPACE` 可改），所有路径解析后必须
  落在工作区树内，越界路径直接拒绝
- **模型**：默认 `zk-auto`（K3 → GLM → 兜底链，走通用池，不碰 flash-lite 专属池）；
  会话创建时可换
- **额度保护**：单任务最多 40 步（`ZKAI_AGENT_MAX_STEPS`）、最多 3 个会话同时跑
  （`ZKAI_AGENT_MAX_CONCURRENT`）、上下文过长自动压缩摘要；每轮调用的路由与 token
  都进控制台「用量统计」，烧了多少一目了然
- **可恢复**：会话与全部过程存 SQLite（`agent_sessions` / `agent_messages` 表），
  刷新页面/重启网关后可回看；网关重启时正在跑的会话标记为「中断」，接着发消息即可续
- **定位**：批量任务跑批 + 网关能力展示窗。日常主力编码仍建议用 ZCode/Claude Code
  （走本网关），Agent 的价值在「挂机跑批量改写」这类无人值守任务

接口面：`/admin/agent/*`（会话 CRUD、审批、SSE 事件流），与控制台共用 admin token；
`ZKAI_AGENT_ENABLED=false` 可整体下线。

---

## 常见问题

**Q：`zk-k3` 是怎么工作的？会试所有渠道吗？**

会。`zk-k3` 只锁定 Kimi K3 这一个模型，但它背后有 **3 个部署**，按顺序全试一遍：

```
商汤 kimi-k3（7 把 Key，7 个账号各一把） → Moonshot 官方（填了 Key 才生效） → NVIDIA moonshotai/kimi-k3
```

实测曾走到第 9 次尝试才在 NVIDIA 成功（当时商汤 5 个账号全 429）。
重试预算已在 `config/config.yaml` 里配好（`max_total_attempts: 20`），**不需要你改任何东西**。

**Q：为什么我用 `zk-k3` 有时报 429 / 失败？**

因为 K3 的免费渠道确实不稳：
- 商汤：`token plan entitlement exhausted` —— K3 走的是**独立 token 套餐**，5 个账号的额度用完就是连续 429（注意：这跟账号余额无关，6.8-flash-lite 照样能用）
- NVIDIA：预览期全局限流，也可能超时

**想让 K3 稳定，只能给 Moonshot 官方充值**，然后填 `.env` 的 `MOONSHOT_API_KEY`（provider 已开好）。否则用 `zk-coding`，它会自动兜底到 GLM/DeepSeek。

**Q：我改了 `.env` 里的模型名怎么没反应？**

因为那里**根本不该配模型名**。模型名只在 `config/models.yaml` 配。`.env` 里写 `XXX_MODEL=` 是无效的，程序不读它也不报错——这是最容易踩的坑，我已经把那几行从 `.env` 里删掉了。

**Q：改了配置要做什么？**

重启网关。托盘图标右键 → 退出，再重新双击 `scripts\start_gateway.cmd`。

**Q：免费模型也在统计费用？那个「等价成本」是什么？**

是"按公开牌价折算的等价成本"，不是真实扣款。商汤/NVIDIA/魔搭这些免费渠道实际 $0，
但网关会按 `config/models.yaml` 里每个部署的牌价（`input_cost_per_mtok` /
`output_cost_per_mtok`，USD/百万 token）算出"这趟如果花钱要多少"，让你知道**省了/值多少**。
控制台「用量统计」和总览的「等价成本」卡都带 ≈¥ 换算。

- 牌价在哪改：`config/models.yaml` 每个部署下两行。目前只有 `kimi-k3` 用的是仓库里
  记录过的官方价（¥20/¥100 每 MTok），**其余是我按同量级填的参考价，标了 `# 参考`，
  请按各家官方价目表核对后改**。
- 成本在**请求发生时**按当时牌价算死，改价只影响之后。历史数据用
  `python scripts\backfill_cost.py`（先预览）→ 加 `--apply` 回填。
- 计费按**真正命中的那个部署**算（K3 走 NVIDIA 就按 NVIDIA 的价，不会串到商汤）。

**Q：怎么知道现在有哪些 Key 可用？**

浏览器打开管理控制台 <http://127.0.0.1:8317/ui> → 「凭据池」页签（状态、成功率、
冷却倒计时、一键解禁一目了然）。想看原始 JSON 就去 <http://127.0.0.1:8317/admin/credentials>。

**Q：ZCode 里连续几十条「思考 · 几秒」正常吗？是不是每换一次 Key 就把前面重新想了一遍？**

正常，而且**不是**网关的锅。一条「思考」对应一次 LLM 往返——Agent 每调用一次工具就要
往返一次，一个复杂任务几十次往返 → 几十条思考，和你用几把 Key 无关（换成官方直连也是
这样，token 也**不会**因换 Key 而累积）。

但网关会尽量让每趟"少重想"：开了**会话亲和**（`credential_affinity`，默认开）后，
同一通对话会钉住上次成功的那把 Key，命中供应商的前缀缓存、历史不重算；只有那把 Key
冷却/挂了才换，换过去后下次成功自动重新锚定。控制台凭据池里的 📌N 就是钉住的会话数。

真正让你感觉"在重新思考"的元凶通常是 429 风暴：额度被扫空 → 反复失败重试 → 每次重试
都从头想。对策就两条：非推理请求别用 `zk-k3`（用 `zk-fast`/`zk-lite` 省额度），以及
别在多个高消耗并行会话间抢同一批 Key。

---

## 出错了怎么排查

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 启动时提示「端口被其他程序占用」 | 别的程序占了 8317 | 按窗口里打印的 PID 确认那个程序；可以关就关；或换端口启动 `start_gateway.cmd 9000`，或改 `.env` 的 `ZKAI_PORT` |
| 启动时提示「无法结束旧实例」 | 旧网关进程权限不足杀不掉 | 托盘图标右键退出重试；还不行就以管理员身份运行，或任务管理器结束对应 python 进程 |
| 右下角找不到托盘图标 | 先确认窗口里没有 `ERROR: the tray exited immediately`——有就是托盘根本没起来（多半是 `.venv` 缺依赖，`uv sync` 解决，日志在 `data\tray_gateway.log`）；没有报错才是 Windows 把新图标收进了「隐藏的图标」折叠菜单（`^` 箭头） | 点 `^` 展开就能看到；想让它常驻可见：任务栏设置 → 选择哪些图标显示在任务栏上 → 把 ZK-AI 打开 |
| 任务栏里还留着一个 `python.exe` / 黑窗口 | 旧版本缺陷：uv 的 venv 启动器会把隐藏标志丢掉，最内层真解释器自己新建了控制台窗口 | 已修复（用 base 真 `pythonw.exe` 启动）；托盘右键退出后重新双击 `scripts\start_gateway.cmd` / `start_burner.cmd` |
| 控制台全站报「管理令牌无效或缺失」 | 你改过 `.env` 里的 `ZKAI_ADMIN_TOKEN`，浏览器里存的还是旧值 | 点右上角「🔑 令牌」，把 `.env` 里那串新的原样粘进去；平时启动会自动带上，只有令牌变了才要手动 |
| 加完供应商后探测模型报错 | 旧版本缺陷：新供应商没注册进路由 | 已修复（重启即可）；若仍报错，看是不是没加 Key 或 Key 的环境变量是空的——探测窗口会直接告诉你缺什么 |
| 调用报 `Connection refused` | 网关没起来 | 回到第 1 步 |
| 调用报 `no_available_credential` | 该 provider 的 Key 全在冷却 | 响应头里带了 `Retry-After`（还差几秒恢复）；等它过去，或去控制台「凭据池」看倒计时 |
| 报 429 | 免费额度限流 | 分钟级限流歇 1 分钟自动恢复；`token plan entitlement exhausted` 是套餐额度烧完，网关会直接长休 30 分钟，别再扫 |
| 上游报 `ConnectError`，但网络明明正常 | 网关出站走 **Windows 系统代理**（httpx 默认读注册表代理，本机是 `127.0.0.1:10808`），代理进程没开时商汤/NVIDIA 全都连不上 | 启动代理软件，或关掉系统代理；`scripts\burn_sensenova.py` 不受影响（它直连商汤） |
| 回复是空白 | 旧版本缺陷 | 已修复（README §18 缺陷 8/9），重启网关即可 |
| 控制台「凭据池」里两把 Key 的掩码一样 | 系统里有个**旧的环境变量**把 `.env` 顶掉了（进程环境变量优先于 `.env`） | 启动日志会打 `... shadowed by ... process environment variables ...`；用 PowerShell 查 `[Environment]::GetEnvironmentVariable('变量名','User')`，删掉或改成和 `.env` 一致后重启 |
