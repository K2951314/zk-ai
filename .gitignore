# --- secrets / local configuration -----------------------------------------
.env
.env.*
!.env.example
# ChatGPT / Codex 桌面版期望配置（本地现役，模板 .example.yaml 可提交）
config/chatgpt.yaml
# 一键换机产物：迁移包与导入前备份都可能含全部 Key，绝不进仓库
exports/
imports_backup/
config/config.yaml
config/providers.yaml
config/models.yaml
config/burner.yaml
config/*.local.yaml

# --- runtime data -----------------------------------------------------------
data/*
!data/.gitkeep
*.db
*.db-wal
*.db-shm
*.sqlite3

# --- python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/
.python-version

# --- tooling ----------------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
junit*.xml
.tox/

# --- editors / OS -----------------------------------------------------------
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- logs -------------------------------------------------------------------
*.log
logs/
config/*.bak*
.zcode/

# --- agent / 调试产物（非交付物）---------------------------------------------
# WorkBuddy 项目数据（会话记忆等），各机器本地生成，不进仓库
.workbuddy/
# 诊断会话留下的临时调试脚本（自带 ruff/mypy 历史错误，去留未定，先排除出提交）
scripts/probe_upstream_path.py
